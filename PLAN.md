# 📋 迭代文档：2048 · AI 托管版

> 本次流程以**迭代**开头：先定 v1.0 的最小可玩范围 → 跑通后复盘 → 再迭代出 v1.1 的 AI 与体验优化。
> 完整流程：**迭代 → AI 开发 → 调试 → Git 提交 → 完成交付**

## 1. 立项：为什么做这个

- **面试要求**做几个小项目，上一个项目（`campus-gpa-helper`）是工具类网页应用，这次换个方向：**网页小游戏**。
- **选择 2048 的理由**：规则人人秒懂（面试官 10 秒能上手）、零依赖零构建、开一局只要 30 秒；
  更重要的是它的核心逻辑是**纯函数状态机**，天然适合单元测试——能把「调试」这一环做扎实。
- **差异化亮点**：普通 2048 满地都是，所以 v1.1 迭代出 **AI 托管**（Expectimax 搜索 + 启发式评估），
  一键让 AI 自己玩到 1024/2048，这是能讲技术的部分。

## 2. v1.0 范围（最小可玩）

**用户故事**

1. 作为玩家，我打开网页就能开始一局 4×4 的 2048，用方向键移动方块。
2. 相同数字相撞合并，分数按合并结果累加。
3. 每次有效移动后随机出现一个新方块（90% 是 2，10% 是 4）。
4. 棋盘满且无法合并时游戏结束；合成 2048 时提示胜利。
5. 最高分保存在本地，刷新不丢。

**验收标准**

- 键盘 ↑↓←→（以及 WASD）都能操作，无效方向的移动**不应**产生新方块。
- 4 个方向的行为对称一致（左右靠翻转、上下靠转置复用同一套滑动逻辑）。
- `[2,2,2,2]` 向左必须是 `[4,4]` + 8 分，**不能**连锁合并成 `[8]` 一次拿 16 分。
- `[2,2,4,0]` 向左必须是 `[4,4,0,0]` + 4 分——新合成的 4 不能立刻和后面的 4 再合并。
- 得分、棋盘状态不依赖 DOM，可以在 Node 里直接断言。

## 3. v1.0 复盘 → 迭代点（v1.1 要解决什么）

跑通 v1.0 后的复盘，找出 4 个明确问题：

| 编号 | 复盘发现的问题 | 迭代方案 |
| --- | --- | --- |
| I-1 | 纯手动操作，玩到 512 就很累，也没有技术亮点 | 加 **AI 托管**：Expectimax + 启发式评估，可调速自动游玩，另加「提示」按钮 |
| I-2 | 手滑点错方向无法挽回 | 加 **撤销**（保存最近若干步棋盘快照） |
| I-3 | 手机上没有方向键 | 加 **触摸滑动** 支持，响应式布局 |
| I-4 | 方块凭空出现、合并无反馈，手感差 | 加 **新方块/合并动画**、分数浮动提示、胜利/失败遮罩 |

**迭代边界**：v1.1 只做以上 4 项，不引入框架、不引入构建工具、不引入任何 npm 依赖——保持「双击即玩」。

## 4. 技术选型

| 选择 | 理由 |
| --- | --- |
| 原生 HTML / CSS / JavaScript | 零依赖零构建，双击 `index.html` 就能玩，面试官无需装环境 |
| 经典脚本 + 全局命名空间（UMD-lite） | ES Module 在 `file://` 下会被 CORS 拦截；经典脚本同时兼容 Node `require`，才能把核心逻辑拿去跑单测 |
| 纯函数核心 `js/board.js` | 不碰 DOM，状态转换 `(board, direction) -> {board, score, moved, merged}`，可在 Node 里穷举边界 |
| AI 独立成 `js/ai.js` | 搜索与评估函数不依赖棋盘全局状态，便于单测与调参 |
| Node 内置 test runner | 零依赖测试，CI 上 `node --test` 即可 |
| 可注入随机源（`rng` 参数） | 测试里用固定序列的伪随机，让「随机出块」也能被断言 |

## 5. 数据模型

- 棋盘 `Board`：`number[4][4]`，`0` 表示空位，其余为 2 的幂。
- 一局状态 `GameState`：`{ board, score, best, history, over, won, keepPlaying }`
- 移动结果 `MoveResult`：`{ board, score, moved, merged }`，其中 `merged` 是合并落点坐标数组，供动画使用。
- 本地存储键：`tile2048.best`（最高分）、`tile2048.state`（当前局，便于刷新续玩）。

## 6. AI 设计（v1.1）

- **算法**：Expectimax（极大极小 + 机会节点）。玩家的移动是 max 节点，随机出块是 chance 节点
  （2 与 4 按 0.9 / 0.1 加权求期望），向下搜 3 层。
- **评估函数**（四项经典启发式加权求和）：
  1. **空格数**：越多越安全；
  2. **单调性**：相邻行列保持同一递增/递减方向，便于把大数堆到角落；
  3. **平滑度**：相邻格子数值差越小越好，避免炸开；
  4. **最大数靠角**：最大方块在角落加分。
- **性能控制**：机会节点只展开前 N 个空格（默认 8 个），把分支因子压下来，保证浏览器里每步在几十毫秒内。
- **可测试性**：AI 不依赖随机数，同一个棋盘输入必然得到同一个决策；测试可断言「有合并机会时必须选择能得分的走法」。

## 7. 测试计划

- 滑动压缩：空位压缩、相邻合并、计分正确
- **合并边界**（最容易写错的地方）：`[2,2,2,2]`、`[2,2,4,0]`、`[4,4,2,2]`、`[8,8,8,8]`
- 方向对称性：四个方向把同一批方块推到对应边缘；上下与左右结果一致
- 无效移动：棋盘无变化时 `moved === false` 且不产生新方块
- 纯函数性：`move` 不得修改传入的棋盘
- 随机出块：固定随机序列下结果可复现；满盘返回 `null`
- 终局判定：满盘无相邻相同 → 无路可走；`hasMoves` 边界
- 胜利判定：出现 2048 即胜利；`maxTile` 正确
- AI：评估函数偏好空格多的局面；有直连合并机会时必须选它；死局返回 `null`；决策确定性；**完整托管一局能正常结束且合出 512 以上**

## 8. Git 提交规范

按迭代阶段分步提交，每步测试都是绿的：`chore(scaffold)` → `docs(plan)` → `feat(core)` → `test` / `fix` → `feat(ui)` → `feat(ai)` → `docs` → `ci`。
每个提交只做一件事，历史可回溯，面试时能拿 `git log` 讲迭代过程。
